spring:
  application:
    name: contribhub
  profiles:
    default: test

api:
  github:
#    auth-token: ${GIT_AUTH_TOKEN}
#    client-id: ${CLIENT_ID}
#    client-secret: ${CLIENT_SECRET}
    auth-token: "test"
    client-id: "test"
    client-secret: "test"
